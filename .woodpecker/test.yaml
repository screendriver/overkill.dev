---
when:
    - event: [push, manual]

steps:
    - name: install
      image: node:26.1.0
      commands:
          - npm clean-install

    - name: validate
      image: node:26.1.0
      commands:
          - npx just validate
